# MCP Tools — Happy Cake Sandbox

> Populated by Claude Code from the team launch kit during T-001.
> Endpoint: `https://www.steppebusinessclub.com/api/mcp`
> Auth: `X-Team-Token` header (value from `STEPPE_MCP_TOKEN` env var, set in `.env.local` only)

## Overview

(CC: paste the launch-kit overview here — what the sandbox simulates, isolation model, rate limits, gotchas)

## Live tool catalog

(CC: paste output of `tools/list` here as JSON or formatted markdown table)

| Tool | Purpose | Inputs | Outputs |
|------|---------|--------|---------|
| _tbd_ | _tbd_   | _tbd_  | _tbd_   |

## Sample calls

(CC: paste 1–3 working curl examples that exercise the tools we'll actually use — catalog read, inventory check, order create, kitchen handoff, etc.)

## Notes & quirks

(CC: anything weird discovered during smoke test — auth header name, response shape, streaming vs. JSON, error codes)
